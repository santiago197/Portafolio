apiVersion: v1
kind: Service
metadata:
  name: portafolio-service
spec:
  selector:
    app: portafolio
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080